{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run make run",
            "type": "node",
            "request": "launch",
            "preLaunchTask": "Run make",
            "program": "${workspaceFolder}/main.js" // Optional placeholder; adjust if needed.
        }
    ]
}